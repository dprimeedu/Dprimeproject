# 2026-06-06 작업 정리 — 학생관리 웹이전(exam·report) 배포·검증 완료

> 운영 브랜치 `main`, NAS auto-deploy. 집에서 짠 exam/report 앱을 회사에서 검증·배포한 날.
> 커밋: `7fde6dec` (이번 주 작업은 `475c1d92`(2026-05-29) 이후 전부).
> 관련 할일 원본: [2026-06-07_회사_할일.md](2026-06-07_회사_할일.md)

---

## ✅ 오늘 완료한 것 (할일 1~6)

### 1. DB 마이그레이션
- 로컬 `makemigrations exam report` → `migrate` → `check` 통과.
- 새 테이블: `exam_paper·exam_question·exam_assignment·exam_session·exam_answer`, `report_classroom·report_student_info·report_daily_record`.
- 진행 중 로컬 env에 matplotlib 없어 `pip install matplotlib` 함.
- `vocab.0006_wordcardstar`는 이미 운영 적용돼 있었음 → **어제(6/5) 낱말카드 별표 기능은 이미 라이브** 확인.

### 2. ⭐ 운영 Docker 의존성 설치 (리포트 PNG 한글)
- `django` 컨테이너에 **matplotlib + fonts-nanum** 설치 + `fc-cache -f`.
- matplotlib이 NanumGothic 인식 확인, 한글 렌더 시 **글리프 누락(두부 □) 0건** 검증.
- ⚠️ **컨테이너 재생성 시 사라짐** → 재설치 필요: `apt-get install -y fonts-nanum && fc-cache -f` + `venv/bin/pip install matplotlib`.

### 3. 배포 (+ 인프라 문제 2건 해결)
- commit `7fde6dec` → push → 운영 반영. config: INSTALLED_APPS(exam,report) + URL include + 토큰 2종 + DEBUG시 MEDIA 서빙.
- **문제①: cron 데몬이 죽어 있었음** → auto-deploy가 안 돌아 push해도 미반영이던 상태. `service cron start`로 복구(정상 가동 확인). *이게 멈춰 있어서 한동안 자동배포가 안 됐음.*
- **문제②**: 컨테이너에서 수동 `git pull` 후 `last-deployed-commit`을 HEAD로 갱신(불필요한 재배포 방지). gunicorn 재시작으로 새 URL/뷰 인식.
- 외부 https로 `/training/exam/`·`/report/`·`/report/list/` 전부 200 확인.

### 4. 내신 정답 웹 업로드 — 9개교 4,712문항
- 스크립트: `TEST 및 답지\일괄출력\TEST 자동화\내신웹업로드.py` (env `project`).
- 정답 xlsx 실제 경로: `교재폴더\내신\고등학교 N학년\개정판\2026년\2026년 1학기 기말고사\{학교}\...(partN)\... 정답\... 변형문제 정답.xlsx`.
  - (참고) 정답 xlsx **생성**은 GUI `완성(코드)\B. 부교재 출력.py`가 통합.xlsx에서 만들어 `내신정답입력`·네트워크 답지폴더로 복사. 웹 업로드와는 별개 단계.
- 드라이런(`PEDU_DRY=1`) 확인 후 실제 POST. 서버 확인: **Papers 17 / Questions 4,712**.
  - 동백고1(Part1·2), 동백고2(Part1), 백현고1(Part1·2), 백현고2(Part1·2), 창의고2(Part1), 청덕고1(Part1·2·내신TEST), 청덕고2(Part1·2), 청덕고3(Part1·내신객관식빈칸), 초당고3(Part1·내신객관식빈칸).
  - `제외`는 서술형(자동채점 불가)+정답이 1~5 아닌 문항.

### 5. 학생 정보 등록 + 시험 배정
- `0. 학생관리자료.xlsx` `학생Data` 시트(이름·학교학년)를 운영 username과 매칭 → **StudentInfo 33명** 자동 생성(학교학년·학교명 파생, 카톡방명은 비워서 기본규칙 `"{이름}({학교}) 프라임에듀 단톡방"` 사용).
  - 33명 전부 1:1 매칭(미매칭·중복 0).
- **시험 배정 40건 / 24명** (학생 학교학년 = 시험지 school_grade 매칭 자동배정).
  - 시험지 미업로드 9개교(동백고3·동백중2·3·어정중3·백현고3) 학생은 배정 0건 — 정상.

### 6. 기능 검증 (비파괴, 운영 shell)
- **응시·자동채점**: 실데이터 3개 시험지에서 정답 전부→만점, 오답 전부→0점. 전 문항 정답 1~5 보유.
- **응시 UX**: OMR식(종이 시험지 보고 웹엔 1~5만 입력) — 의도된 설계(`session.html`: "번호별로 답을 선택한 뒤 제출").
- **리포트 PNG**: 테스트 레코드로 생성 OK(165KB), 한글 글리프 누락 0, font=NanumGothic. (검증 후 삭제)
- **자동채움**: 코드패스 크래시 없음. vocab/요약/영작/**모의고사(→독해과제1)** 자동연동. **내신은 의도적으로 자동채움 제외**(카테고리 혼합 → 교사 입력).

---

## ⬜ 남은 것 — 이따 TEST 해볼 항목

### 7. 카톡 전송 (회사 PC, 데스크톱 카톡 필요)
```powershell
cd "...\완성(코드)\권용국 작업"
python 리포트받기_웹.py            # 바탕화면\카카오톡 에 png 다운로드
python Kakaosend.py               # 단톡방 전송
# 또는 한번에: python 리포트받기_웹.py --send
```
- [ ] png 다운로드 확인 → 단톡방 전송 확인 (리포트 카톡 API: `GET /report/api/kakao/today/?date=&token=`, REPORT_KAKAO_TOKEN)

### 브라우저로 직접 눈으로 볼 것 (선택)
- [ ] 학생 계정 로그인 → `/training/exam/` 에 배정된 시험 뜨는지 → 응시·제출·채점 화면
- [ ] 교사 `/report/` 일일입력 그리드 → 저장
- [ ] `/report/` "시험결과 자동 채움" 버튼 (단어/요약/영작 세션 있는 날)
- [ ] `/report/list/` "전체 리포트 생성" → 미리보기 한글 OK

---

## 📌 보류 / 후속 결정 (집에서 이어서)
- **내신 exam → 리포트 필드 매핑**: 내신TEST/객관식빈칸/변형 점수를 리포트 어디에 표시할지 (모의고사만 독해과제1 자동연동, 내신은 보류 → 현재 교사 수동입력).
- **변형문제 대량 문항 응시 UX**: Part1 458~721문항이 한 페이지에 너무 많음 → 회차/단원 분할 필요.
- **일일입력 그리드** 20열로 넓음 → 반별 필터 등 개선.

## 🔗 협력자 공유 링크
- 이번 주 전체: `https://github.com/dprimeedu/Dprimeproject/compare/475c1d92...main`
- 오늘 배포(exam/report)만: `https://github.com/dprimeedu/Dprimeproject/commit/7fde6dec`
