# 2026-06-07 (회사) 할 일 — 학생관리 웹이전 검증·배포

> 2026-06-06 집에서 작성한 코드(①~⑤ + 시험결과 자동연동) 검증·배포.
> 전부 문법검증만 됨(집엔 Django/matplotlib 없음). **실행·migrate·배포는 회사에서.**
> 상세 설계: [학생관리_웹이전_설계.md](학생관리_웹이전_설계.md) §11~13.

---

## 0. 먼저 — 동기화 확인
- [ ] SynologyDrive 동기화 완료 확인 (집 작업물이 회사 PC에 다 들어왔는지)
- [ ] 새로 추가된 것 확인:
  - 앱 `exam/` (모의고사·내신 응시/채점)
  - 앱 `report/` (일일기록·리포트·카톡)
  - `config/settings.py`(INSTALLED_APPS: exam, report / 토큰 3종), `config/urls.py`
  - 스크립트: `TEST 자동화\내신웹업로드.py`, `완성(코드)\권용국 작업\리포트받기_웹.py`

## 1. DB 마이그레이션
```powershell
conda activate project
cd "...\Dprimeproject"
python manage.py makemigrations exam report
python manage.py migrate
python manage.py check        # 에러 없는지 먼저 확인
```
- [ ] `check` 통과 / migrate 성공 (새 테이블: exam_paper·exam_question·exam_assignment·exam_session·exam_answer / report_classroom·report_student_info·report_daily_record)

## 2. 리포트용 Docker 환경 (④ PNG 생성)  ⭐중요
- [ ] 운영 Docker 이미지에 **matplotlib + 한글폰트(NanumGothic)** 설치 확인
  - 없으면 리포트 글자가 □□□(두부)로 나옴
  - 예: `apt-get install -y fonts-nanum` + `pip install matplotlib`, 폰트캐시 갱신
- [ ] (선택) 토큰 환경변수 — 안 넣으면 기본값 사용:
  - `EXAM_IMPORT_TOKEN`(기본 pedu-exam-2026), `REPORT_KAKAO_TOKEN`(기본 pedu-report-2026)

## 3. 배포
- [ ] git add/commit/push → NAS auto-deploy (평소 방식)
- [ ] 운영 사이트 정상 뜨는지 확인

## 4. 내신 정답 업로드 (②)
```powershell
cd "...\TEST 및 답지\일괄출력\TEST 자동화"
$env:PEDU_DRY="1"; python 내신웹업로드.py     # ① 먼저 검증(POST 안 함)
Remove-Item Env:PEDU_DRY; python 내신웹업로드.py   # ② 실제 업로드
```
- [ ] 드라이런 결과 확인 (9개 학교 정답파일 — 백현고1 Part1 458 등)
- [ ] 실제 업로드 성공 (✅ 메시지)
- [ ] 특정 학교만 시 `$env:PEDU_NAESHIN_SCHOOL_FILTER="백현고2"`

## 5. 학생 정보 등록 (③④⑤ 전제)
- [ ] 관리자(/admin) → **StudentInfo** 채우기: 학생별 **학교학년**(리포트 헤더) + **카톡 단톡방명**(전송 매칭, 비우면 "이름 프라임에듀 단톡방")
- [ ] 모의고사/내신 시험지 **학생 배정** (`/training/exam/` 교사화면 → 배정)

## 6. 기능 검증 (운영 또는 로컬)
- [ ] **학생 응시**: `/training/exam/` → 모의고사·내신 응시(1~5) → 제출 → 즉시 채점·점수·오답
- [ ] **교사 일일입력**: `/report/` → 날짜 선택 → 출결·과제 입력 → 저장
- [ ] **시험결과 자동채움**: `/report/` 상단 "시험결과 자동 채움" → 단어/요약/영작/모의고사 점수 자동 입력 확인
- [ ] **리포트 생성**: `/report/list/` → "전체 리포트 생성" → 미리보기(한글 깨짐 없는지 ★)

## 7. 카톡 전송 (⑤)
```powershell
cd "...\완성(코드)\권용국 작업"
python 리포트받기_웹.py            # 바탕화면\카카오톡 에 png 다운로드
python Kakaosend.py               # 기존 방식 전송
# 또는 한번에: python 리포트받기_웹.py --send
```
- [ ] png 다운로드 확인 → 단톡방 전송 확인

---

## 결정·확인 필요 (집에서 이어서 할 것)
- [ ] **내신 exam → 리포트 필드 매핑**: 내신TEST/객관식빈칸/변형문제 점수를 리포트 어느 칸에 표시할지 (모의고사는 독해과제1로 자동연동 됨, 내신은 보류 중)
- [ ] **변형문제 대량 문항 응시 UX**: Part1 458~721문항 → 한 페이지에 너무 많음. 회차/단원 분할 필요
- [ ] 일일입력 그리드 20열로 넓음 → 반별 필터 등 개선

## 참고 (토큰·URL)
- 내신 import: `POST /training/exam/api/import-naesin/` (EXAM_IMPORT_TOKEN)
- 리포트 카톡: `GET /report/api/kakao/today/?date=&token=` (REPORT_KAKAO_TOKEN)
- 교사: `/training/exam/`(시험) · `/report/`(일일입력) · `/report/list/`(리포트)
