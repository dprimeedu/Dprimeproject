# 2026-06-02 작업 정리 & 테스트 체크리스트

> 운영 사이트: https://dprimeedu.synology.me/  · 운영 분기: **main** (오늘부터)
> 학생 로그인: ID `primeedu1`~`primeedu33` / 비번 `123456` (상세: [단어훈련_학생ID목록.md](단어훈련_학생ID목록.md))

---

## A. 오늘 한 작업 (전부 라이브 반영 완료)

### 1. 단어훈련(vocab) 앱 신규 — Quizlet식
- 재원생 메뉴(`/training/`)를 **[영작훈련, 단어훈련]** 2카드로 재구성
- **대전 입장**을 재원생 메뉴 top에서 빼고 **영작훈련 홈 안**으로 이동
- 단어훈련: 단어↔뜻 **플래시카드** + 방향 토글 + **⭐별표만(모르는 단어) 집중훈련**(서버 저장)
- 단어 import 커맨드(`import_vocab`): 통합본 `내신단어` 시트 → 단원/단어

### 2. 포맷된 PC 가상환경 재구축
- conda env `project`(Python 3.10) 재생성 + requirements 전체 설치

### 3. 운영 분기 문제 복구 ⚠️
- 라이브가 협력자 `main`을 돌고 있고 auto-deploy가 멈춰 있었음(fast-forward 실패)
- `feature/dprime-rewrite`(단어훈련) → `main` **충돌 0으로 머지** → 배포 → migrate → auto-deploy를 `main` 추적으로 복구
- **이제 운영 분기는 `main`. push 시 1분 자동 배포 정상.**

### 4. 학생 계정 33개
- ID 규칙 `primeedu`+순번 / 비번 `123456` (전원 재원생 승인)
- **primeedu3: 권용국 → 김은재 교체**
- 중학생 6 (primeedu1~6, 기존) + **고등학생 27 신규 (primeedu7~33)**

### 5. 학교 단어장 8개 import + 배정
- 동백고1·동백고2·백현고1·백현고2·창의고2(교과서)·청덕고1·청덕고2·초당고3 = **11,151+단어**
- **20명 배정** (학교 매칭) / 미배정 7명(동백고3·백현고3·청덕고3 — 통합본에 단어 없음)
- 백현고2는 통합본이 학생 시트와 달라 **학생 시트 기준 1986단어로 재구성**

### 6. 교사용 단어 관리 UI
- 사이드바(관리자) **[단어 단원 관리]** + **[단어 학생 배정]**
- 단원별/학생별 배정 모달, 학생 일괄 등록(엑셀)

### 7. 별표(모르는 단어) 이식
- 학생 구글시트(링크 공개) `내신단어` D열 체크 → **StudentWordStar**
- 5명 **341개** 이식 (조성하 80·문준혁 59·송지우 43·장시은 139·이채원 20)

---

## B. 테스트 체크리스트 (직접 확인 부탁드립니다)

> 아직 실사용 테스트 전이라, 아래를 한 번씩 눌러보시면 됩니다.

### B-1. 재원생(학생) 화면
- [ ] **로그인** — `primeedu7` / `123456` 로 로그인되는지 (다른 번호도 동일 비번)
- [ ] 로그인 후 **재원생 메뉴**에 카드 2개(**영작훈련 / 단어훈련**)만 보이는지 (대전 입장 카드는 없어야 함)
- [ ] **영작훈련 홈** 들어가서 위쪽에 **대전 입장 코드 입력칸**이 옮겨와 있는지
- [ ] **단어훈련** → 배정된 학교 단어장 카드가 보이는지 (primeedu7=동백고1)
- [ ] 플래시카드: **카드 클릭/스페이스로 뒤집기**, 좌우 이동, **방향 토글**(뜻 먼저 보기)
- [ ] 카드 우상단 **별표(☆) 눌러보기** → 새로고침해도 유지되는지 (서버 저장)
- [ ] **⭐별표만 토글** → 별표한 카드만 나오는지

### B-2. 별표 이미 들어간 학생(이식 확인)
- [ ] `primeedu23`(장시은, 백현고2) 로그인 → 단어훈련 → **별표만 토글 → 139개** 나오는지
- [ ] `primeedu20`(문준혁) → 별표 **59개**, `primeedu28`(이채원, 청덕고2) → **20개**
- [ ] 별표된 단어가 실제로 그 학생이 구글시트에 체크했던 "모르는 단어"와 맞는지 (몇 개 눈으로)

### B-3. 교사(관리자) 화면  ※ 관리자 계정으로 로그인
- [ ] 사이드바 관리자에 **[단어 단원 관리]**, **[단어 학생 배정]** 보이는지
- [ ] **단어 단원 관리**: 단원 8개 + 단어수/배정학생수 표시, **[학생 배정]** 버튼 모달에서 체크/저장
- [ ] **단어 학생 배정**: 학생 목록 + 검색, **[단어장]** 버튼 모달에서 학생에게 단원 배정/해제
- [ ] **학생 일괄 등록**(엑셀 업로드) 양식 다운로드 + 업로드 동작
- [ ] 미배정 7명(양지후·윤정민·정찬·권순형·이기백·정안나·최회빈)에게 임시로 아무 단원 배정해보고 학생화면 반영 확인 → 다시 해제

### B-4. 영작훈련(기존 기능 회귀 확인)
- [ ] 영작훈련이 기존대로 정상 동작하는지 (단어훈련 추가가 영작에 영향 없는지)
- [ ] 협력자의 변형문제/부교재 출력 등 기존 기능 정상인지

### B-5. 단어장 내용 점검 (중요)
- [ ] 각 학교 단어장의 **단어·뜻이 실제 그 학교 내신과 맞는지** 표본 확인
  - 특히 통합본↔학생시트가 다를 수 있음(백현고2 사례). 동백고2·청덕고2는 100% 일치 확인됨.

---

## C. 알려진 미완 / 다음 후보
- **미배정 7명**: 동백고3·백현고3·청덕고3 통합본에 `내신단어` 데이터가 비어 단어장 없음 → 단어 채워지면 import 후 [단어 학생 배정]에서 배정
- **중학생 단어장**: primeedu1~6은 중학교 통합본 import 시 배정
- **단어 TEST 모드**(뜻↔단어 입력 채점, Quizlet식): 아직 안 만듦 — 다음 후보
- **영어 문제 채점 시스템**(로드맵 2단계, 구글 Apps Script 대체): 통합본 `문제출력` 시트 기반. 큰 작업 — 다음 후보

---

## D. 내일 할 일 (우선순위순)

### 0. (먼저) 오늘 작업 테스트 — 위 **B 체크리스트** 한 바퀴
- [ ] 학생/교사 화면 직접 눌러보고 이상한 점 메모 → 내일 같이 수정

### 1. 미배정 7명 단어장 채우기
- [ ] 동백고3·백현고3·청덕고3 통합본 `내신단어` 시트가 비어 있음 → **단어 데이터 채우기**(완성코드 자동화 쪽) 또는 학생 시트 기준으로 import
- [ ] import 후 [단어 학생 배정]에서 양지후·윤정민·정찬·권순형·이기백·정안나·최회빈 배정
- 대상 학생: primeedu16·24·25·29·30·31·32

### 2. 단어장 내용 정확도 점검 (학교별)
- [ ] 백현고2처럼 **통합본↔학생시트가 다른 학교**가 더 있는지 확인 (0체크라 미검증인 학교들)
- [ ] 다르면 학생 시트 기준으로 단원 재구성 (백현고2와 동일 방식)

### 3. 중학생 단어장
- [ ] 중학교 통합본 import → primeedu1~6 배정 (동백중2/3, 어정중3)

### 4. 다음 기능 결정 — 둘 중 택1 (테스트 후)
- [ ] **(A) 단어 TEST 모드** — 뜻 보고 단어 입력 → 서버 채점 + 점수/XP (vocab에 추가, 작음)
- [ ] **(B) 영어 문제 채점 시스템** — 통합본 `문제출력`(문제·지문·보기·정답) → 객관식 풀이·서버채점·오답, 구글 Apps Script 대체 (큼, writing 세션급)
  - 첫 슬라이스 후보: "번호별 답 입력 + 서버 채점"(현 워크플로 미러) · 객관식 유형부터

### 5. 협력자 공유
- [ ] 운영 분기가 **main**으로 통합됐고 단어훈련(vocab) + migration 1건 추가됐음을 협력자에게 공유 (이후 둘 다 main에 작업)

