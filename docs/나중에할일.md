# 나중에 할 일 (모음)

> 여러 작업·메모에 흩어져 있던 "추가로 해야 할 일"을 한 곳에 모았다.
> 부르면 여기부터 보면 됨. 정리일: 2026-06-12.

---

## 🔴 시험(exam) — 데이터 업로드 확장

### 1. 지문·관련번호·해설 DB 업로드  ⭐바로 이어서
- **상태**: 웹 코드는 작성 완료(아직 **커밋·배포 안 함, 로컬에만**). 모델 필드(`ExamQuestion.ref_number/text/explanation`) + 마이그레이션 `exam/0004` + import API 확장 + 교사 결과페이지 '문항 자료(해설)' 표시까지 됨. 가짜데이터로 검증만 함(롤백).
- **남은 일**:
  - [ ] 웹 쪽 배포: `git add exam/ && commit && push` → NAS migrate `exam`
  - [ ] **푸시 도구(`TEST 자동화\내신웹업로드.py`) 수정** — 한 문항을 `[번호, 정답, 유형, 관련번호, 지문, 해설]`(또는 dict `{number,answer,qtype,ref_number,text,explanation}`)로 전송. 안 보내면 예전처럼 번호·정답·유형만(하위호환).
- **목적**: 나중에 틀린번호만 모아 해설까지 같이 보는 오답 DB 활용.

### 2. 시험일·하루목표 자동 업로드
- **상태**: `ExamPaper.exam_date`(시험일), `daily_goal`(하루목표) 필드 있음. 지금은 **Django admin에서 수동 입력**만 가능.
- **남은 일**:
  - [ ] import API가 `exam_date`·`daily_goal`도 받도록 확장
  - [ ] 푸시 도구가 엑셀의 하루목표(예 70)·시험일을 같이 전송 → admin 수동입력 제거
- **참고**: 시험 응시 화면 하단바의 '오늘 목표 N'·'시험 D-day'·'남은 수업 N번'이 이 값으로 채워짐.

### 3. 내신 exam 점수 → 리포트 필드 매핑
- 내신TEST/내신객관식빈칸/변형문제(Part1~4) 점수를 일일리포트 어느 칸에 표시할지 미정.
- 모의고사는 '독해과제1'로 자동연동됨. 내신은 보류 중.
- (출처: docs/2026-06-07_회사_할일.md)

---

## 🔴 모의고사 빨파 (데이터 적재 완료 → UI/분류 남음)

- ✅ **데이터 적재 완료**(2026-06-12): 모고 빨파 270시험지·7,771문항 + 해설이미지 6,600장 운영 DB에. (source=naesin, category=빨파)
- ✅ **빨파 응시 UI 완료**(2026-06-12): session이 category=빨파면 리딩 레이아웃(지문+[빨]/[파] 지시문+정답입력). 자동저장·2차·해설이미지 재사용.
- ✅ **모의고사 분류 완료**(2026-06-12): 내신 목록서 빨파 제외, 전용 회차 브라우저 `/training/exam/mock-redblue/`(학년→연도·월). 자기주도 응시(배정 불필요).
- [ ] 이미지 없는 문항(7,771 중 1,171) — 빨파정답 PNG가 없던 회차. 일단 놔둠(사용자), 필요시 PDF→PNG 추가 변환.
- ✅ **빨파정답 학생 공개 = 교사 2차 확인 후**(2026-06-16 완료): `ExamSession.redblue_released`(+_at, exam 0007). 교사 결과페이지 '빨파정답 공개' 버튼(`release_redblue`) → 학생 결과페이지에 빨파 정답이미지 노출. 그전엔 '선생님 확인 대기'.
- ✅ **모고↔빨파 2차 연동**(2026-06-16 완료): 일반 모고(QuestionData) 1차 채점 → 오답 번호의 빨파 문제(ExamQuestion 지문[빨]/[파])로 2차 재풀이. `redblue_qmap_for_mock()`이 회차(school_grade+season='YYYY M월 모의고사')+번호로 1:1 매칭. 듣기 등 빨파 없는 문항은 graceful.
- [ ] (참고) 일반 객관식 직접 풀이는 QuestionData 흐름 그대로(보기 포함). 모고 2차에 원문 보기(5지선다)도 같이 노출할지는 미정 — 현재는 빨파 지문만.

### 모의고사 회차별 단어DB(MockVocab) — 빠진 회차 채우기  ⭐단어 더블클릭
- ✅ **단어DB + 지문 더블클릭 완료**(2026-06-16): 빨파 지문에서 영단어 더블클릭 → 회차·문항 맥락뜻 팝업(MockVocab→없으면 Gemini) + 개인 낱말카드 자동저장. 엑셀 적재 103,917행 / 263회차. 재업로드: `import_mock_vocab` 커맨드(메모리 `project_mock_vocab_db` 참고).
- [ ] **단어DB 완전 누락 11개 회차** — 엑셀에 단어 채워 재업로드 필요. (현재는 이 회차 전부 Gemini 폴백):
  - 고2 2025년 3월 · 6월 · 9월 · 11월
  - 고3 2022년 9월 · 11월
  - 고3 2024년 3월 · 4월 · 11월
  - 고3 2025년 4월 · 7월
- [ ] **거의 비어있는 회차 4개**(1문항만 들어있음 = 사실상 누락): 고3 2023년 3월 · 6월 · 7월 · 10월
- 채운 뒤: `python manage.py import_mock_vocab --xlsx "…단어 DB.xlsx" --jsonl-out z:/mock_vocab.jsonl` → `ssh dprime-nas '… import_mock_vocab --stdin' < /z/mock_vocab.jsonl` (특정 회차만 뜻수정은 `--only 학년:년도:월 --replace`).

## 🔴 오답·유사유형·자동출제 (2026-06-15 요청)

### 4. 내신 시험 오답 보기 + 유사 유형 풀기 (exam)
- **목표**: 학생이 자기 내신(시험) **오답만 모아보고**, 그 오답과 **비슷한 유형 문제를 풀어** 보충.
- 현재: exam은 1차/2차 재시험(`ExamAnswer.round`)까지만. 오답 모아보기·유사유형 출제 없음.
- 남은 일:
  - [ ] 학생 결과/홈에 **'내 오답 모음'**(틀린 문항 + 해설) — 위 #1 지문·해설 DB 업로드와 연결
  - [ ] **'비슷한 유형 풀기'** — 같은 유형(qtype)/관련번호 문제를 자동 모아 출제(로컬 변형문제 풀 또는 유형 매칭)

### 5. 어법 오답 다시 풀기 + 유사 유형 풀기 (신규 어법 앱)
- **선행 필요**: 웹에 **어법(문법) 시험 앱이 아예 없음**(→ `docs/학생관리표_웹이전.md`). 먼저 어법 과목이 있어야 함.
- **설계 문서**: `docs/어법앱_웹이전_설계.md` (구글 어법시험 → 웹 이전. 원본 형식·모델·응시/채점·단계별 계획). 2026-06-15 작성, **응시/채점 방식 확정 후 착수**.
- 남은 일:
  - [ ] 어법 문제 모델/응시/채점 (단어·요약문·영작·시험처럼 한 과목 추가) 또는 exam에 어법 유형 편입
  - [ ] 어법 **오답 다시 풀기**(재시험)
  - [ ] 어법 **비슷한 유형 풀기**(유형별 변형문제 출제)

### 6. 학교 내신과 유사한 문제 자동 출제
- **목표**: 각 학교 내신 시험과 **유사한 문제를 자동 생성**해 출제(시험 대비).
- 로컬 변형문제 파이프(변형문제검수 등) 활용 → 요약문/영작처럼 **웹으로 푸시 + 학교 학생 자동배정**.
- 남은 일:
  - [ ] 로컬: 학교 통합본 기준 유사/변형 문제 생성 배치
  - [ ] 웹: exam(또는 신규) import + 학교·학년 자동배정(기존 `member.auto_assign` 재사용)

---

## ✅ 접근 권한(아이디별) 시스템 (2026-06-15 요청 → 2026-06-16 완료)

**완료(2026-06-16)**: `Member.academy_access` 4단계(migration `member/0005`,`0006`): `none`/`variant_view`(변형 열람만)/`variant_down`(변형 열람+다운로드)/`full`(모고 전체). 프로퍼티 `is_admin_level`(=is_staff/superuser만)/`can_view_mock_full`/`can_view_variant`(열람)/`can_download`(다운로드). 게이트: 열람(academy_list·academy_list_result·exam_list_result)=`require_variant`, 다운로드(download_pdf·download_modified_hwpx)=`require_download`, 중요영작=`require_mock_full`. variant 계정은 변형문제 카테고리만 노출/허용. base.html 모고 메뉴=`can_view_variant`, 관리자 메뉴=is_staff/superuser만, exam_list_result 다운로드 버튼=`can_download`만. **학원 셀프가입(is_academy): 가입 즉시 is_active=True + `academy_access='variant_view'`(변형 열람만). 관리자가 access-admin에서 `variant_down`으로 올리면 다운로드 허용(=승인).** 전용 관리 화면 `/access-admin/`(등급 토글·저장, 학원/재원생 뱃지, 관리자 대상 변경 거부). Django admin list_editable. 로컬 테스트 통과(열람학원=다운403/다운학원=다운200/관리자=전체).

### 8. 계정 유형별 메뉴/콘텐츠 접근 제한 + 관리자 설정 화면 (완료)
- **요청 내용(사용자 원문 요약)**:
  1. `primeedu1`~`primeedu33` 아이디 = **우리 학원 재원생** → **재원생 메뉴만** 보이게. **모고 데이터 다운로드 접근 X**.
  2. **모고 데이터 다운로드** = **관리자만 전체** 볼 수 있게.
  3. 관리자가 **승인한 (외부) 아이디**는 **변형문제만** 볼 수 있게 제한.
  4. 관리자가 **아이디별로 "어디까지 볼 수 있는지" 설정하는 화면(공간)** 을 만들기.

- **현재 구조(조사 결과, 2026-06-15)**:
  - 계정모델 `member/models.py` `Member`: `login_id`(재원생 ID, 'primeedu1'…), `is_approved`(재원생 승인), `is_academy`(학원운영자), `is_staff/is_superuser`(관리자), `member_type`(user/academy_admin/admin). **접근레벨/권한 범위 필드는 아직 없음 → 추가 필요**.
  - 메뉴 노출 로직: `config/templates/base.html:71-112` (is_approved/is_staff/is_academy로 사이드바 분기). "모고 데이터 다운로드"는 현재 **미승인 학생/관리자에게 표시**(base.html:72) — 재원생(is_approved=True)에겐 이미 안 보이지만, primeedu 학생이 URL 직접 접근은 가능.
  - academy 앱(모고/변형문제) 뷰 접근제어가 느슨함: `academy_list`/`academy_list_result`는 **데코레이터 없음(누구나)**, `exam_list_result`/`download_modified_hwpx`/`download_pdf`는 **`@login_required`만**(모든 로그인 사용자). → **권한 게이트 필요**.
  - `/mock-exam/`(=academy_list, 모고 리스트)와 `/exam_list_result/`(변형문제 등 문제목록), `/download_modified_hwpx/`, `/download_pdf/` 가 제한 대상.

- **해야 할 일(설계 초안)**:
  - [ ] `Member`에 **접근범위 필드** 추가(마이그레이션). 예: `access_level`(none/variant_only/mock_full) 또는 boolean 묶음(`can_view_mock`, `can_view_variant` 등) + 무엇까지 볼지. 재원생(primeedu*) 기본 = 재원생 메뉴만/모고 차단.
  - [ ] **재원생 자동 판별**: login_id가 `primeedu\d+` 패턴이면 재원생으로 간주해 모고/변형 접근 기본 차단(또는 가입/승인 시 플래그 세팅).
  - [ ] **뷰 권한 게이트**: academy 모고 뷰들에 권한 데코레이터/체크 추가 — 관리자=전체, 승인된 외부아이디=변형문제만, 재원생=차단. (`exam_list_result`에서 category가 '변형문제'가 아니면 비관리자/제한계정 차단 등.)
  - [ ] **base.html 메뉴 노출**도 권한 필드 기준으로 정리(모고 데이터 다운로드는 권한 있는 계정에만).
  - [ ] **관리자용 권한 설정 화면**: 아이디별로 접근범위를 토글/선택해 저장(예: writing `student_admin` 패턴 재사용하거나 member admin 확장, 또는 academy 전용 관리 페이지 신규). Django admin(`member/admin.py`)만으로도 임시 가능하나, 사용자 요청은 **전용 설정 공간**.
- **참고**: UI(모고 리스트/변형문제 다운로드 버튼)는 2026-06-15에 새로 정리됨 — 권한만 얹으면 됨.

---

## 🟡 게임화·동기부여

### 7. 단어/요약문/시험 게임화(XP·배지) 4과목 통합
- **상태**: 레벨·XP·연속출석·배지는 **영작(writing) 전용**(StudentProfile). 학생 리포트(`/report/me/`)의 레벨/XP도 영작 기반.
- **문제**: 영작 안 하고 단어/요약문/시험만 하는 학생은 그 부분이 비어 보임.
- **남은 일**: [ ] 4과목 통합 XP/레벨 체계로 확장.

---

## ✅ 최근 해결됨 (참고)
- 변형문제 대량 문항 응시 UX(한 페이지 너무 많음) → **OMR 격자(한 줄 10개, 자동 다음칸)로 해결** (2026-06-12)
- 영작 단원 분할 들쭉날쭉 → **세트 패킹(고15/중20) + 분할단원 자동통합** (2026-06-12)
- 시험 복수정답/오류표시/이어풀기/2차 재시험/틀린번호 집계 (2026-06-12)
